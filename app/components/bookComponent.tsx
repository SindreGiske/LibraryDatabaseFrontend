import {Box} from "@navikt/ds-react";

export default function bookComponent(
    id: number,
    title: string,
    author: string,
    loaned: boolean,
    disableLoaned: boolean
) {

    return (
        <Box>
            

        </Box>
    )

}